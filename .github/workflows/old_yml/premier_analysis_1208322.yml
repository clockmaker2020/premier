name: premier_analysis_1208322

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  run-analysis:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Run analysis script
        run: |
          echo "🔍 Running analysis for match 1208322"
          # python scripts/analyze_match.py --match_id 1208322
